# KingHost Passenger Configuration for FastAPI
# This file configures Apache/Passenger to run the FastAPI application

# Enable Passenger
PassengerEnabled On

# Set the application root directory
# Replace 'vrdsolution' with your actual KingHost username
PassengerAppRoot /home/vrdsolution/www/vrdchecklist.com.br

# Specify the startup file (WSGI entry point)
PassengerStartupFile passenger_wsgi.py

# Set application type to WSGI (for Python/FastAPI)
PassengerAppType wsgi

# Path to Python interpreter in virtual environment
# Replace 'vrdsolution' with your actual KingHost username
PassengerPython /home/vrdsolution/.local/share/virtualenvs/checklist/bin/python3

# Environment settings
SetEnv PASSENGER_APP_ENV production
SetEnv PASSENGER_STARTUP_FILE passenger_wsgi.py
SetEnv PYTHONPATH /home/vrdsolution/www/vrdchecklist.com.br

# Passenger process settings
PassengerMinInstances 1
PassengerMaxPoolSize 6
PassengerPoolIdleTime 300
PassengerMaxRequests 1000

# CORS Headers for API
<IfModule mod_headers.c>
    # Allow cross-origin requests from your frontend domain
    Header always set Access-Control-Allow-Origin "https://www.vrdsolution.com.br"
    Header always set Access-Control-Allow-Methods "GET, POST, PUT, DELETE, PATCH, OPTIONS"
    Header always set Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With"
    Header always set Access-Control-Allow-Credentials "true"
    Header always set Access-Control-Max-Age "3600"
    
    # Security headers
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

# URL Rewriting
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    
    # Handle preflight OPTIONS requests
    RewriteCond %{REQUEST_METHOD} OPTIONS
    RewriteRule ^(.*)$ $1 [R=200,L]
    
    # Don't rewrite if file or directory exists
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    
    # Rewrite all other requests to passenger_wsgi.py
    RewriteRule ^(.*)$ passenger_wsgi.py/$1 [QSA,L]
    
    # Force HTTPS
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</IfModule>

# Deny access to sensitive files
<FilesMatch "^\.">
    Order allow,deny
    Deny from all
</FilesMatch>

<FilesMatch "\.(env|ini|log|sh|sql)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Enable compression
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
    AddOutputFilterByType DEFLATE application/json
</IfModule>

# Set proper MIME types
<IfModule mod_mime.c>
    AddType application/json .json
    AddType application/javascript .js
    AddType text/css .css
</IfModule>

# Error documents
ErrorDocument 404 /404.html
ErrorDocument 500 /500.html
